# Database connection (DSN format for MySQL)
DSN="mysql://myuser:mypassword@localhost:myport/mydatabase"

# Login credentials for the application
LOGIN="mylogin"
PASSWORD= "mypassword" # Password associated with the above login

# URL for the production environment
URL_PROD="https://www.myproductionurl.com"

# SMTP configuration for sending emails
SMTP="smtp.mailtrap.io" # Example SMTP server (replace with your provider)
smtp_port=

# Email addresses
sender="noreply@mydomain.com"      # Sender email address
recipient="support@mydomain.com"   # Recipient email address 
